import { Router } from 'express';

/**
 * Tenant-scoped shop router, mounted at /api/v1.
 * Sub-routers (auth, products, sales, ...) are added in later sprints.
 */
const router = Router();

export default router;
